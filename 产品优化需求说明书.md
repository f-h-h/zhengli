# 家庭收纳位置管理系统｜产品优化需求说明书

## 文档说明

本文档基于原始需求规格说明书，从产品用户体验角度梳理优化点，所有优化均在现有核心架构基础上做增量增强，不改变「业务归属-几何坐标分离」、「嵌套深度限制3层」等核心设计原则。

---

## 一、用户体验与交互优化

### 1.1 坐标输入体验优化

**问题描述**：
- 当前要求用户计算并输入中心点坐标，手动录入时有认知负担和计算错误风险
- CSV导入支持墙角偏移自动换算，但手动编辑界面未提供

**优化方案**：
- UI层全部改为「墙角偏移+底部离地」输入方式
- 输入项：`corner_x`（X方向墙角偏移）、`corner_z`（Z方向墙角偏移）、`bottom_y`（底部离地高度）
- 前端自动换算中心点：
  ```
  centerX = cornerX + w / 2
  centerZ = cornerZ + d / 2
  centerY = bottomY + h / 2
  ```
- 数据库仍存储中心点`x/y/z`，不改变存储结构
- 保留手动编辑中心点的入口，满足高级用户需求

**验收标准**：
- 用户无需手动计算中心点，降低学习成本
- 编辑已有数据时正确回显corner值

---

### 1.2 3D拖拽吸附对齐优化

**问题描述**：
- 当前只支持自由拖拽，用户很难对齐到墙面或相邻容器
- 手动微调坐标耗时

**优化方案**：
- 增加「吸附对齐」开关，默认开启
- 支持两种吸附：
  1. **贴墙吸附**：拖拽时距离墙面小于阈值（0.05m）自动贴齐
  2. **边对齐吸附**：距离相邻容器的边小于阈值时自动对齐
- 吸附后视觉有反馈提示

**验收标准**：
- 拖拽时自动吸附，布局效率提升
- 开关可关闭，满足自由摆放需求

---

### 1.3 树形导航+面包屑双模式

**问题描述**：
- 当前只有面包屑展示当前链路，无法整体浏览收纳结构
- 深层节点跳转不方便

**优化方案**：
- 左侧边栏增加完整收纳树形视图
- 展示结构：房间 → 顶层容器 → 子容器 → ...
- 支持折叠/展开
- 点击树节点：
  1. 3D场景自动聚焦并高亮该容器
  2. 面包屑同步更新
  3. 打开详情面板
- 保留原有面包屑导航，双模式并存

**验收标准**：
- 可以快速定位任意层级容器
- 树形结构与业务parent_id关系一致

---

## 二、核心功能增强

### 2.1 物品搜索与快速定位

**问题描述**：
- 当前缺失搜索功能，无法快速找到物品位置
- 系统核心价值之一就是"找东西"，搜索是高频刚需

**优化方案**：
- 顶部增加搜索框，支持全文搜索物品名称
- 搜索结果列表展示：物品名称 → 所属容器 → 收纳路径
- 点击搜索结果：
  1. 自动展开树形导航到对应节点
  2. 3D场景高亮对应容器
  3. 面包屑定位

**验收标准**：
- 输入关键词后秒出结果
- 点击能快速定位到3D场景

---

### 2.2 常用家具模板快速创建

**问题描述**：
- 当前所有容器都需要用户手动逐个创建，初始化效率低
- 常见家具结构有规律可复用

**优化方案**：
- 新建容器时支持「从模板创建」选项
- 内置模板：
  | 模板名称 | 默认结构 |
  |----------|----------|
  | 平开衣柜 | 柜体(cabinet) + N个抽屉隔间(drawer) + 顶面(table) |
  | 床头柜 | 桌面(table) + 抽屉(drawer) |
  | 置物架 | N层搁板(shelf_layer) |
- 用户选择模板后，只需调整整体尺寸和层数，系统自动创建所有容器

**验收标准**：
- 一键创建常见结构，初始化时间从分钟级降到秒级

---

### 2.3 容器复制粘贴

**问题描述**：
- 创建多个相同尺寸容器时需要重复输入
- 效率低下

**优化方案**：
- 容器详情增加「复制」按钮
- 复制后点击「粘贴」创建新容器
- 复制内容：类型、尺寸、颜色，`parent_id`默认同父，支持修改
- 名称自动加序号后缀

**验收标准**：
- 快速创建多个相似容器

---

### 2.4 图片附件功能

**问题描述**：
- 当前只有文字名称，时间长了用户记不清容器/物品具体是什么
- 尤其是换季收纳的物品，需要视觉提示

**优化方案**：
- 容器详情支持上传1张封面图
- 物品详情支持上传1-3张图片
- 3D场景点击容器后，详情面板显示缩略图
- 点击图片可查看原图

**存储方案**：
- 图片文件存储在服务端，路径记录在数据库

---

## 三、数据管理优化

### 3.1 CSV导入错误处理优化

**问题描述**：
- 当前导入只要有一行错误，整批都回滚
- 大数据量导入时，用户修正错误成本高

**优化方案**：
- 导入模式选择：
  1. **全有或全无**（原有逻辑）：出错整批回滚
  2. **正确先行**（新模式）：正确数据先导入，错误行返回行号+错误信息，用户修正后单独补导入
- 无论哪种模式，都返回完整错误报告

---

### 3.2 全量数据备份与恢复

**问题描述**：
- 当前缺失系统级备份能力
- 存在数据丢失风险

**优化方案**：
- 后台增加「备份」按钮，一键导出全量数据
- 导出格式：JSON打包所有房间、locations、items
- 支持导入备份文件恢复数据
- 导入选项：覆盖现有数据 / 追加导入

---

### 3.3 跨房间移动容器

**问题描述**：
- 当前容器锁定在创建时的房间，无法整体移动
- 家居调整后需要重新创建所有数据

**优化方案**：
- 容器详情支持修改`room_id`
- 移动时自动级联更新所有子容器的`room_id`
- 级联更新容器下所有物品的`room_id`（通过容器关联）
- 给出确认提示，防止误操作

---

## 四、物品录入体验优化

### 4.0 物品录入效率优化

**问题描述**：
- 当前物品录入需要每次选择容器，批量录入同一个容器时操作重复
- 没有快速复用机制，相同或相似物品重复录入成本高

**优化方案**：

1. **同容器连续录入模式**
   - 进入容器详情页后，提供「批量添加物品」入口
   - 打开批量添加面板后，容器已预选锁定
   - 用户只需要输入物品名称，点击「保存并新增」或按回车直接创建下一个
   - 无需每次重新选择容器，大幅提升批量录入效率

2. **物品快速复制**
   - 物品详情增加「复制为新物品」按钮
   - 复制后保留：名称（加后缀）、分类、尺寸、标签等信息
   - 用户只需微调即可保存，适合多个相似物品

3. **常用物品模板库**
   - 用户可将常用物品保存为个人模板
   - 新建物品时可从模板选择，自动填充所有字段
   - 系统内置常见物品默认模板（衣物、书籍、鞋盒等）带预设尺寸

4. **输入联想提示**
   - 输入物品名称时，下拉联想历史录入过的相似名称
   - 用户点击即可快速填入，减少打字量

5. **批量勾选操作**
   - 容器物品列表支持多选
   - 支持批量删除、批量移动到其他容器

---

### 4.1 物品基础信息扩展（为后续搭配功能铺垫）

**优化方案**：
- 物品支持录入三维尺寸 `item_w`/`item_d`/`item_h`（单位：米），可选填
- 增加物品分类字段 `category`：衣物/书籍/电子/厨具/工具/饰品/药品/其他
- 支持多标签功能，用户自定义标签
- 保持原有逻辑：不强制要求填写尺寸，用户可选择只记录位置

**价值**：
- 为后续「尺寸匹配推荐」「智能搭配」打下基础
- 用户可以提前判断"这个物品能不能放进这个抽屉"

---

### 4.2 换季收纳标签

### 4.3 换季收纳标签

**适用场景**：换季衣物收纳，冬季收夏装、夏季收冬装

**优化方案**：
- 物品表增加`season_tag`字段
- 可选值：`常年`、`春季`、`夏季`、`秋季`、`冬季`
- 支持按季节筛选物品列表
- 3D视图可筛选显示当季物品所在容器

---

### 4.4 物品附加信息

**优化方案**：
- 物品表增加字段：
  - `purchase_url`：购买链接
  - `purchase_date`：购买日期
  - `price`：购买价格
- 这些字段均为可选，用户按需填写

---

### 4.5 取放时间记录

**优化方案**：
- 增加物品`last_access_date`字段
- 用户取出/放回物品时可手动更新时间
- 支持按「超过X年未访问」筛选，帮助用户清理长期闲置物品

---

## 五、后续扩展方向：物品细节还原与智能搭配

### 5.1 物品3D可视化还原

**扩展方向**：
- 录入尺寸的物品，支持在3D场景中渲染
- 真实展示容器内部物品摆放状态
- 直观呈现剩余空间，帮助用户规划

**价值**：
- 从"记录位置"升级到"还原真实收纳状态"
- 可视化展示空间利用情况

---

### 5.2 智能搭配推荐功能

**核心价值**：解决用户"买了新物品，不知道放哪合适"的痛点

**推荐维度**：
1. **尺寸搭配** → 输入物品尺寸，系统自动推荐有足够空隙的容器
2. **分类搭配** → 同分类物品优先推荐就近空位
3. **使用频率搭配** → 常用物品推荐易取位置，不常用推荐深处/高处
4. **季节搭配** → 当季物品推荐易取位置，过季推荐收纳到深处

**设计原则**：
- 只给推荐建议，不强制用户接受
- 保持用户最终决策权，系统只减少思考成本

---

### 5.3 分阶段演进路线

```
第一阶段（当前）：物品录入优化 + 基础信息扩展（尺寸、分类）
    ↓
第二阶段：物品3D可视化渲染容器内摆放
    ↓
第三阶段：智能搭配推荐算法上线
```

---

### 5.4 场景增值：厨房场景全链路智能推荐

**适用场景**：冰箱收纳食材、橱柜收纳厨具

**扩展方向**：

1. **食材管理基础**
   - 物品分类增加「食材干货」子类：蔬菜、水果、肉类、海鲜、干货、调料等
   - 记录购买日期、保质期、剩余数量
   - 临近保质期提醒，避免浪费

2. **双层菜谱推荐逻辑**
   - **根据已有食材推荐菜谱**：冰箱/储物柜现有哪些食材，推荐可以做的菜
   - **根据已有厨具推荐菜谱**：用户现有什么锅具/厨具，推荐能用这些厨具做的菜

3. **智能购买建议**
   - 选定菜谱后，自动对比"已有食材"和"菜谱需要食材"
   - 列出缺料清单，推荐需要购买的食材

**价值**：
- 解决"冰箱里有这些食材，不知道做什么菜"的日常痛点
- 解决"买了新锅不知道做什么菜"问题
- 形成完整闭环：**收纳存放 → 库存盘点 → 菜谱推荐 → 缺料采购**
- 差异化特色功能，大幅提升产品粘性

---

## 六、统计与智能化辅助

### 6.1 空间利用率统计面板

**优化方案**：
- 房间首页增加统计Dashboard：
  1. **已使用容积占比**：所有可收纳容器容积之和 / 房间总容积
  2. **容器使用率排行**：哪些容器放得满，哪些空置
  3. **空容器提醒**：列出没有物品的空容器
  4. **物品总数统计**：按类型统计

---

### 6.2 几何一致性检查提示

**现状**：当前不校验子容器是否在父容器几何内部，允许不一致

**优化方案**：
- 增加可选的「一致性检查」功能
- 保存容器时，检查子容器边界是否超出父容器几何范围
- 如果超出，给出提示「子容器几何位置超出父容器范围，是否继续保存？」
- 不强制拦截，仅提示，由用户判断
- 保持原设计灵活性

---

### 6.3 可用空间推荐（进阶）

**优化方案**：
- 用户新增容器时，系统分析房间内闲置空隙，推荐可放置位置
- 推荐基于容器尺寸匹配空闲区域
- 仅作为推荐，由用户决定是否采纳

---

## 七、移动端适配

### 7.1 移动端响应式浏览

**问题描述**：
- 用户外出时可能需要查询"这个东西放哪了"
- 当前需求未考虑移动端

**优化方案**：
- 至少支持移动端响应式布局：
  1. 树形导航浏览
  2. 搜索物品
  3. 查看物品详情和收纳位置
  4. 查看容器图片
- 3D渲染在移动端可降级为2D平面图
- 编辑创建功能可留到桌面端完成

---

## 八、优先级排期建议

| 优先级 | 优化模块 | 投入 | 用户价值 | 投入产出比 |
|--------|----------|------|----------|-----------|
| 🔴 P0 | 1.1 墙角偏移输入 | 低 | 高 | 极高 |
| 🔴 P0 | 1.3 树形导航 | 中 | 高 | 高 |
| 🔴 P0 | 2.1 物品搜索 | 中 | 高 | 高 |
| 🔴 P0 | 4.0 物品录入效率优化 | 中 | 高 | 极高 |
| 🔴 P0 | 4.1 物品基础信息扩展 | 低 | 高 | 高 |
| 🔴 P0 | 7.1 移动端浏览 | 中 | 高 | 高 |
| 🟡 P1 | 1.2 吸附对齐 | 中 | 中 | 中高 |
| 🟡 P1 | 2.2 模板创建（容器） | 中 | 中 | 中高 |
| 🟡 P1 | 2.4 图片附件 | 中 | 中 | 中 |
| 🟡 P1 | 3.2 备份恢复 | 低 | 高 | 中高 |
| 🟡 P1 | 4.0 常用物品模板 | 中 | 中 | 中高 |
| 🟢 P2 | 2.3 容器复制粘贴 | 低 | 中 | 中 |
| 🟢 P2 | 3.1 导入错误优化 | 低 | 中 | 中 |
| 🟢 P2 | 3.3 跨房间移动 | 低 | 中 | 中 |
| 🟢 P2 | 4.3 换季标签 | 低 | 中 | 中 |
| 🟢 P2 | 6.1 空间统计 | 中 | 中 | 中 |
| 🟢 P2 | 6.2 一致性检查 | 低 | 中 | 中 |
| 🔵 P3 | 4.4 附加信息 | 低 | 低 | 低 |
| 🔵 P3 | 4.5 取放记录 | 低 | 低 | 低 |
| 🔵 P3 | 6.3 位置推荐 | 高 | 中 | 低 |
| 🔵 P3 | 5.x 物品3D+智能搭配 | 高 | 高 | 中（后续阶段） |

---

## 九、数据库表结构变更

### 架构预留：多租户（多用户）支持

为后续支持多用户场景预留架构，所有表统一增加 `user_id` 字段：

```sql
-- rooms 表增加用户ID
ALTER TABLE rooms ADD COLUMN user_id INTEGER;
-- locations 表增加用户ID
ALTER TABLE locations ADD COLUMN user_id INTEGER;
-- items 表增加用户ID
ALTER TABLE items ADD COLUMN user_id INTEGER;
```

**设计说明**：
- 当前阶段仅作架构预留，单用户场景`user_id`固定为1即可
- 后续需要支持多家庭成员共享时，直接基于`user_id`做数据隔离，无需改表结构
- 不影响现有单用户使用流程，只是预留扩展空间

### locations表新增字段

```sql
-- 图片封面
ALTER TABLE locations ADD COLUMN cover_image TEXT;
```

### items表新增字段

```sql
-- 物品尺寸（可选）
ALTER TABLE items ADD COLUMN item_w REAL;         -- X方向宽度
ALTER TABLE items ADD COLUMN item_d REAL;         -- Z方向深度
ALTER TABLE items ADD COLUMN item_h REAL;         -- Y方向高度
-- 分类和标签
ALTER TABLE items ADD COLUMN category TEXT;       -- 物品分类
ALTER TABLE items ADD COLUMN tags TEXT;           -- 自定义标签，JSON数组
-- 食材相关（扩展用）
ALTER TABLE items ADD COLUMN expiration_date DATE;-- 保质期到期日
ALTER TABLE items ADD COLUMN quantity REAL;       -- 剩余数量
ALTER TABLE items ADD COLUMN unit TEXT;           -- 单位：个/斤/克/袋
-- 图片、季节标签、购买信息、取放时间
ALTER TABLE items ADD COLUMN images TEXT;         -- JSON数组存储图片路径
ALTER TABLE items ADD COLUMN season_tag TEXT;     -- '常年','春季','夏季','秋季','冬季'
ALTER TABLE items ADD COLUMN purchase_url TEXT;
ALTER TABLE items ADD COLUMN purchase_date DATE;
ALTER TABLE items ADD COLUMN price REAL;
ALTER TABLE items ADD COLUMN last_access_date DATE;
```

---

## 十、总结

本次优化遵循以下原则：

1. **不改变核心架构**：业务归属与几何坐标分离的设计保持不变
2. **增量增强**：在原有功能基础上增加能力，不破坏向后兼容
3. **用户为中心**：重点解决搜索、定位、录入效率等核心痛点
4. **可选功能**：新增检查、推荐等功能都保持可选，不强制用户使用

P0-P1级别优化完成后，产品用户体验将得到显著提升。
